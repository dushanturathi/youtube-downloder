version: '3.8'

services:
  youtube-downloader:
    build: .
    container_name: youtube-downloader

    ports:
      - "5000:5000"

    volumes:
      - ./downloads:/app/downloads

    restart: unless-stopped
